# 咔鏘 - RWA 多鏈支付平台 🚀

基於 **Next.js + Wagmi + RainbowKit** 打造的 Web3 多鏈支付平台，採用 **咔鏘 黑灰金色主題**設計。

## ✨ 功能特色

- 💰 **多鏈支付** - 支援 ETH 原生代幣及 ERC-20 代幣支付
- 📱 **收款功能** - 生成支付 QR Code，方便收款
- 📤 **付款功能** - 支援掃描 QR Code 或上傳圖片付款
- 🦊 **多錢包支援** - imToken、MetaMask、Rainbow、WalletConnect
- 💼 **資產管理** - 側邊欄查看 ETH 和 ERC-20 代幣餘額
- 🎨 **黑灰金色主題** - 現代化 UI 設計，專業且優雅
- 📱 **完全響應式** - Mobile First 設計，支援所有裝置

## 🛠️ 技術堆疊

- **框架**: [Next.js 14](https://nextjs.org/) (App Router)
- **Web3**: [Wagmi v2](https://wagmi.sh/) + [RainbowKit](https://www.rainbowkit.com/)
- **樣式**: 自訂 CSS（基於 咔鏘 設計系統）
- **語言**: TypeScript
- **QR Code**: qrcode, jsQR

## 🎨 設計系統

### 顏色配置

```css
--bg: #0b0d10         /* 黑灰背景 */
--gold: #9E844A       /* 指定金色 */
--text: #e6eaf0       /* 主要文字 */
--muted: #a4afbd      /* 次要文字 */
--line: #232a33       /* 分隔線 */
```

## 📋 前置需求

1. **Node.js** (v18 或以上)
2. **npm** / **yarn** / **pnpm**
3. **錢包** (MetaMask / imToken / 任何 WalletConnect 相容錢包)

## 🚀 快速開始

### 安裝依賴

```bash
npm install
```

### 啟動開發伺服器

```bash
npm run dev
```

### 開啟瀏覽器

前往 [http://localhost:3000](http://localhost:3000)

## 📁 專案結構

```
rwa_test/
├── app/
│   ├── components/
│   │   ├── WalletNav.tsx          # 錢包導航（側邊欄）
│   │   ├── ReceivePayment.tsx     # 收款組件
│   │   └── SendPayment.tsx        # 付款組件
│   ├── globals.css                # 全域樣式（咔鏘 主題）
│   ├── layout.tsx                 # 根 Layout
│   ├── page.tsx                   # 首頁
│   └── providers.tsx              # Context Providers
├── lib/
│   └── wagmi.ts                   # Wagmi + RainbowKit 配置
├── package.json
└── README.md
```

## 🎯 核心功能說明

### 1. 錢包連接 & 資產顯示

- 使用 RainbowKit 提供的多錢包連接
- 側邊欄顯示 ETH 餘額和 ERC-20 代幣（USDT, USDC, DAI）
- 自動更新餘額（每 10 秒）

### 2. 收款功能

- 輸入金額（可選）
- 生成支付 QR Code
- 包含 EIP-681 支付 URI

### 3. 付款功能

- 開啟相機掃描 QR Code
- 上傳 QR Code 圖片
- 自動解析支付資訊
- 發送交易並追蹤狀態

## 🔧 自訂配置

### 新增支援的代幣

編輯 `app/components/WalletNav.tsx` 中的 `COMMON_TOKENS`：

```typescript
const COMMON_TOKENS = [
  {
    symbol: 'LINK',
    name: 'Chainlink',
    address: '0x514910771AF9Ca656af840dff83E8264EcF986CA',
    decimals: 18,
    chainId: 1,
  },
  // ... 更多代幣
]
```

### 修改支援的網路

編輯 `lib/wagmi.ts`：

```typescript
import { polygon, arbitrum } from 'wagmi/chains'

export const config = createConfig({
  chains: [mainnet, sepolia, polygon, arbitrum],
  // ...
})
```

## 📱 HTTPS 需求

**相機功能需要 HTTPS 環境**。開發時可以使用：

### 選項 1: ngrok

```bash
ngrok http 3000
```

### 選項 2: localhost（Chrome）

Chrome 將 `localhost` 視為安全來源，可直接使用相機。

## 🐛 常見問題

### Q: 相機無法啟動？

**A:** 確保：
1. 使用 HTTPS 或 localhost
2. 已授予瀏覽器相機權限
3. 使用支援 getUserMedia API 的瀏覽器

### Q: 代幣餘額顯示不正確？

**A:** 確認：
1. 已連接到正確的網路（Ethereum Mainnet）
2. 錢包確實持有該代幣
3. RPC 端點正常運作

### Q: 如何在手機上測試？

**A:** 
1. 使用 ngrok 生成公開 HTTPS URL
2. 在手機上開啟該 URL
3. 使用手機錢包掃描或連接

## 🔐 安全提醒

⚠️ **重要：**

1. **永遠不要分享私鑰或助記詞**
2. **在主網測試前先用測試網**
3. **這是範例專案，生產環境需進行安全審計**
4. **保護好 WalletConnect Project ID**

## 📚 學習資源

- [Wagmi 文件](https://wagmi.sh/)
- [RainbowKit 文件](https://www.rainbowkit.com/)
- [Next.js 文件](https://nextjs.org/docs)
- [EIP-681: URL 格式支付請求](https://eips.ethereum.org/EIPS/eip-681)

## 📄 授權

MIT License

## 💬 支援

有問題或建議？歡迎開 Issue 討論！

---

**Happy Coding! 🎉**
